import { createFileRoute } from "@tanstack/react-router";

export const Route = createFileRoute("/onboarding/_layout/subscription-plan")({
  component: () => <div>Hello /onboarding/_layout/subscription-plan!</div>,
});
